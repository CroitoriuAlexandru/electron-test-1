const {
  BrowserWindow,
  app,
  ipcMain,
  Notification,
  BrowserView,
  screen,
} = require("electron");
// const db = require("./../dbo/models");
const path = require("path");


// module.exports = { createWindow };
